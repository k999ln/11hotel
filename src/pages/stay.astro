---
// 一覧ページは /vip（フィルター・購入機能つきの市場ボード）に統合（2026-07-29）。
// ?place=... は /vip 側で同じ絞り込みとして解釈されるようクエリ文字列ごと引き継ぐ。
// 個別トークンの詳細ページ（/stay/[tokenId]）は引き続き残す。
return Astro.redirect(`/vip${Astro.url.search}`, 301);
---
